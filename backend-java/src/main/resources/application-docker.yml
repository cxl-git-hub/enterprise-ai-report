spring:
  datasource:
    url: jdbc:mysql://mysql:3306/ai_report?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: report
    password: report123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: redis
      port: 6379
      password: redis123456

jwt:
  secret: ${JWT_SECRET:myDefaultSecretKeyForJwtTokenGeneration2024!}

minio:
  endpoint: http://minio:9000
  access-key: minioadmin
  secret-key: minioadmin123
  bucket-name: ai-reports

ai:
  service:
    url: http://ai-service:8081

logging:
  level:
    com.enterprise.report: INFO
    com.baomidou.mybatisplus: WARN
